name: restaurant_ui
description: "Restaurant System — shared Flutter design system (theme, tokens, status colours) for the merchant and customer apps."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  restaurant_domain:
    path: ../domain

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
